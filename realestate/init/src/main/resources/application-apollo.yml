app.id: ${spring.application.name}
apollo:
  bootstrap:
    enabled: true
    namespaces: application,app-url,redis,rabbitmq,ribbon,hystrix,threads,config,dzgx,bdcdj-db
  meta: http://apolloconfig:18080
/**
 * 是否已缴费
 * @type {boolean}
 */
var sfyjf = false;

/**
 * 验证缴费状态(南通需求，此处默认不实现)
 * @param processInsId
 * @returns {*}
 */
function checkJfzt(processInsId) {
    var deferred = $.Deferred();
    sfyjf = true;
    deferred.resolve();
    return deferred;
}

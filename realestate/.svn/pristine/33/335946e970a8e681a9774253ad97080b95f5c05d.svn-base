package cn.gtmap.realestate.register.ui.web.rest;

import cn.gtmap.realestate.register.ui.App;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.test.context.web.WebAppConfiguration;

import static org.junit.Assert.*;

/**
 * @author wyh
 * @version 1.0
 * @date 2022/3/8 10:21
 */
@RunWith(SpringRunner.class)
@SpringBootTest(classes = App.class)
@WebAppConfiguration
public class BdcBdcdyControllerTest {
    @Autowired
    BdcBdcdyController bdcBdcdyController;

    @Test
    public void checkSflzrcf() {
        bdcBdcdyController.checkSflzrcf("3785535");
    }
}
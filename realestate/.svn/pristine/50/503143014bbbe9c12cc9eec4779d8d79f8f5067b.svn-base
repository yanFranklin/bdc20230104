/**
 * 蚌埠：自定义查询——1.0登记业务查询 页面登记历史查看JS处理
 */
function djls(obj, data) {
    if(!data || !data.XMID) {
        layer.msg('当前记录xmid数据，无法查看！');
        return;
    }

    //window.open('/realestate-accept-ui/rest/v1.0/slym?processInsId='+data.GZLSLID+'&readOnly=true');
    window.open('/realestate-register-ui/view/lsgx/new-page.html?xmid=' + data.XMID + '&processInsId=' + data.GZLSLID + '&version=bengbu');
}
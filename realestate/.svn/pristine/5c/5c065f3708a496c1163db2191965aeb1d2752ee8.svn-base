package cn.gtmap.realestate.exchange;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.ComponentScan;
import org.springframework.context.annotation.FilterType;

/**
 * 舒城上报功能内网应用
 */
@ComponentScan(basePackages = "cn.gtmap.realestate.exchange", excludeFilters = {@ComponentScan.Filter(type = FilterType.ASSIGNABLE_TYPE)})
@SpringBootApplication
public class ReportAloneApp {

    public static void main(String[] args) {
        SpringApplication.run(ReportAloneApp.class, args);
    }

}
